import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Recuperar Senha · Zelvo',
}

export default function RecuperarSenhaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
